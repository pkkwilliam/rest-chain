const {
  generatePreviousRequestVariable,
} = require("./previousRequestGenerator");
const {
  validatePreviousRequestVariable,
} = require("./previousRequestValidator");

module.exports = {
  generatePreviousRequestVariable,
  validatePreviousRequestVariable,
};
